<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd">

	<!-- Root Context: defines shared resources visible to all other web components -->
	<!-- 등록해서 공통으로 사용할 파일 등록 -->
	<!-- 1./WEB-INF/jdbc.properties -->
	<bean id="propertyConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholerConfigurer">
		<property name="locations">
			<list>
				<value>../WEB-INF/views/dbinfo/jdbc.properties</value>
			</list>
		</property>
	</bean>

	<!-- 2.jdbc.properties 파일에서 각 각의 키를 분리 -->
	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
	</bean>

	<!-- 3.(Mybatis) bean등록(SqlSessionFactoryBean) 1)configLaction -> 테이블에 대한 
		xml 파일을 불러 올 때 2)dataSource -> DB연결정보 멤버변수 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="configLocation" value="WEB-INF/SqlMapConfig.xml" />
		<property name="dataSource" ref="dataSource" />
	</bean>


	<!-- 4.SqlSessionTemplate(sqlSession 객체를 더 쉽게 얻어오기 위해서 설정) -->
	<bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg index="0" ref="sqlSessionFactory" />
	</bean>
	

</beans>
